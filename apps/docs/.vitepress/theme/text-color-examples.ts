export const textColorExamples: Record<string, string> = {
  'tc-semantic': `<div class="flex flex-col gap-2 p-3 bg-surface rounded-md">
  <p class="text-default">.text-default — primary text</p>
  <p class="text-secondary">.text-secondary — secondary text</p>
  <p class="text-tertiary">.text-tertiary — tertiary text</p>
  <p class="text-placeholder">.text-placeholder — placeholder text</p>
  <p class="text-disabled">.text-disabled — disabled text</p>
</div>`,

  'tc-status': `<div class="flex flex-col gap-2 p-3 bg-surface rounded-md">
  <p class="text-primary">.text-primary — brand primary</p>
  <p class="text-success">.text-success — success</p>
  <p class="text-warning">.text-warning — warning</p>
  <p class="text-error">.text-error — error</p>
  <p class="text-info">.text-info — info</p>
</div>`,
};
